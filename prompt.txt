Review my GitHub RAG project carefully:
https://github.com/Touqeer3417/corrective-rag-

I have identified a possible issue in the streaming implementation:

"chat_stream() first runs the full CRAG graph, including LLM answer generation, then discards that answer and calls generate_stream() again. This causes duplicate LLM generation and delays the first SSE token."

Verify this issue from the actual code and fix it properly at a production level.

Requirements:
- Do NOT rewrite the whole project.
- Inspect only the files relevant to the CRAG graph, streaming, chat service, LLM generation, and API endpoint.
- Streaming must execute retrieval/CRAG logic only once and generate the final answer exactly once using true token streaming.
- The first SSE token should be sent as soon as generation starts.
- Keep the existing CRAG architecture and functionality intact.
- Avoid unnecessary API calls, duplicate processing, and blocking operations.

For every file that must change:
1. Give the exact file path.
2. Explain briefly why it needs to change.
3. Provide the COMPLETE updated code for that file, not snippets.
4. Clearly mention if any file does not need modification.

Do not guess. Base everything on the actual repository code.